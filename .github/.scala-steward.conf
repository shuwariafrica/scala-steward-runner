# Default for every managed repo, merged with (and overridden by) that
# repo's own .scala-steward.conf if it has one.

# Matches Renovate's commitMessagePrefix (shuwariafrica/.github default.json).
# ${default} preserves scala-steward's own title construction; this only
# prepends to it.
commits.message = "[dependencies] ${default}"
